[
{% for post in posts %}
{
    "title": "{{ post.title}}", "description":"{{ post.description}},"etajnost":"{{ post.etajnost}}",
    "floor":"{{ post.floor}}", "price":"{{ post.price}}", "rooms":"{{ post.rooms}}", "type":"{{ post.type}}",
    "area":"{{ post.area }}"}}",
    }
{% endfor %}]
]